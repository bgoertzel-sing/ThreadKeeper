{
  "persona_file": "prompt-researcher.txt",
  "provider": "Ollama-local",
  "model": "granite4.1-30b-16k",
  "base_url": "http://localhost:11434/v1",
  "api_key_env": "OLLAMA_API_KEY",
  "max_output_tokens": 1500,
  "default_tool_subset": ["search", "read-file"],
  "notes": "ThreadKeeper LOCAL WORKER — runs on granite-30b-16k on the .248 RTX 3090. The delegate skill routes heavy sub-task work here so it costs $0 (own hardware). The control loop (DeepSeek) only supervises."
}
